const Input = ({id, name, className, type, placeholder}) => {
    return(
        <input id={id} name={name} className={className} type={type} placeholder={placeholder}/>
    )
}

export default Input